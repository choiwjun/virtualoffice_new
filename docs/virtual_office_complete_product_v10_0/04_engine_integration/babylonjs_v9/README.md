# Babylon.js v9 Integration

Load a rigged GLB with `VirtualOfficeAvatar.load`, then call `play('ANIM_IDLE_001')` or another clip ID. Add `DefaultRenderingPipeline`, SSAO2, shadows and GlowLayer for the intended office presentation.
