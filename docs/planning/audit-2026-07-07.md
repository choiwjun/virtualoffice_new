# docs/ 기획 대비 구현 전수조사 (2026-07-07, authoritative)

> 📦 **아카이브 — D27 전환(2026-07-08) 이전 시점 스냅샷.** 아키텍처 관련 서술은 현행(D27)과 다르며, 특정 시점 기록으로만 참조할 것. 코드 실측 수치(pytest 등)는 작성 시점 기준 유효.

> `$team` 3-lane(worker) 감사 + 리더 실측 교정. 워커 원본은 `artifacts/audit/raw-lane-*.md`(정확도 편차 있어 리더가 현재 소스로 전면 재검증).
> 기준 커밋 `c1b06b5`. 검증 방법: 라우트/모델/서비스 직접 grep + 컨테이너 기동 확인.

## 0. 워커 감사 정확도 정정 (중요)
worker-1(Lane A)이 아래를 "미구현"으로 오판 → **실측 결과 전부 구현됨**:
- APScheduler: `main.py:30 start_scheduler()` lifespan + `scheduler.py` 3잡(18:00 KPI push, 21:00 AI, 매시간 ERP) 등록·기동 ✅
- `POST /api/maps/generate|validate` ✅ (maps.py), `POST /api/auth/login` ✅, `GET /api/seat-assignments` ✅, office-layouts D12 7종 ✅(worker-2 확인), 좌석편집기 도구·스키마 draft ✅
worker-2(Lane B) API 계약 감사는 정확하나 미완(데이터/ERP/KPI 섹션 누락, 요약 TBD). worker-3(Lane C)는 provider timeout으로 실패 → Lane C는 리더가 spec-conformance 결과로 대체.

---

## 1. 구현 완료 (실측 확인)
- **API 계약**: management-api.yaml 34경로 중 ~28 구현 + 다수 신규(daily-status-push POST/retry, org-groups CRUD/validate/deploy, maps, seat CRUD, floors, action-items, auth/me, wa/*). office-layouts D12(validate/deploy/rollback) 7/7.
- **스케줄러(D17/D18)**: 등록·기동. 단 **잡 본문**은 아래 §2 외부의존 부분.
- **KPI 엔진**: 결정론적·**반감시 D14 (a)~(e) 완전 준수** ✅.
- **좌석편집기(REQ-003)**: 벽/방/구역/문 도구 + D12 validate 통과 draft ✅.
- **프론트 11화면 + 명세 컴포넌트**: spec-conformance 7스토리로 정합(work-log 일일리포트·employees presence/좌석·kpi AI초안/진행바/팀랭킹·meetings 캘린더/액션·sync 재시도·office-layout 도구·org-chart CRUD/검증/배포). 라이브 QA 8/8.
- 인증(OIDC+JWT), presence 7종, ERP sync soft-delete, 감사로그 자동기록.
- 검증 지표: pytest 412 passed, npm build EXIT=0.

---

## 2. 미구현 (외부 의존 · Phase 5~7) — 실제 잔여
| 항목 | 정본 | 상태 | 심각도 |
|---|---|---|---|
| STT 회의록 초안(D5/REQ-006) | 06/12-tasks P5, 10-roadmap Ph5 | stt-draft 501 스텁, S2 PoC 미실행 | HIGH |
| AI 서술 초안 **생성**(REQ-007) | 08-kpi-logic, D17 | 표시 UI는 구현, **생성(Claude API)·가명화 미배선** | HIGH |
| 실 ERP push **전송**(REQ-008/D18) | 03-erp, D17 | daily_status_push 테이블·POST·retry·스케줄 잡 있음, **잡 본문의 실 ERP 전송은 스텁**(실 ERP DB 미접속) | HIGH |
| WA Room API·고급 scripting(Phase 4) | 10-roadmap Ph4, D24 | LiveKit 토큰만, 변수 push·이벤트 훅·명시입장 다이얼로그 미구현 | HIGH |
| WA 맵 상호작용(WAM areas/entities) | 07-3d, Ph3 | map_generator 실제 오피스 생성 ✅, 회의존 WAM areas 배선 부분(TMJ zones+presence.js) | MED |
| D20 컴플라이언스 | D20 (a)~(f) | presence 30일 파기·녹음 동의배너·AI 가명화·보존기한·ERP 최소수집 VIEW 미구현 | MED |
| 회의 예약 충돌 검증(D23) | D23/REQ-005 | 예약+FCFS 스키마만, 시간·room 중복 검증 미구현 | MED |
| PUT /seat-assignments | management-api | 배정 수정 API 미구현 | MED |
| 부하/성능(20인)·HG-SEC 하드닝·백업 리허설 | 01-prd, HG | 미수행 | MED |

---

## 3. 문서 드리프트 (D26 미반영) — 즉시 조치 가능, 코드 무관
| 문서 | 갭 | 심각도 |
|---|---|---|
| docs/3d-design/scene-structure.md | Godot 씬·room_builder.gd 참조, WA TMJ 미반영 | HIGH |
| docs/3d-design/asset-registry.md | ASSET_CATALOG GDScript·pak, WA 타일셋 PNG 미반영 | HIGH |
| docs/3d-design/optimization-criteria.md | GTX1650·Godot Forward+ 수치, WA 브라우저 기준 미반영 | HIGH |
| specs/screens/virtual-office-3d.yaml | D26 전환 주석 없음(platform: godot-native) | HIGH |
| docs/planning/02-trd-architecture.md | §2.2 Godot 헤드리스 서버 절 잔존 | MED |
| docs/planning/11-tech-stack.md | Godot/GDScript 스택 절 잔존 | MED |
| docs/planning/01-prd.md | Godot 품질 수치(60fps@GTX1650 등) 잔존(D22는 WA로 대체) | LOW |
(참고: docs/api/realtime-server-api.yaml·office_layout_to_godot.py·godot/ 는 이미 삭제 완료)

---

## 4. 계약 드리프트 (management-api.yaml vs 구현) — 문서 정렬 필요
- `/users`↔`/api/employees` 경로명 + `{items,total}` envelope·role/team 필터 미구현
- adjust·objections/review: 계약 PUT vs 구현 POST
- work-logs·minutes·meetings 수정: 계약 PUT vs 구현 PATCH
- release: 계약 `{assignment_id}` vs 구현 `{seat_id}`
- `/erp-sync/trigger`→`/erp/sync`; login 201 vs 200
- **미문서 신규 엔드포인트**(문서화 필요): daily-status-push(+retry), org-groups CRUD/validate/deploy, maps generate/validate, seat CRUD, floors, action-items, auth/me, wa/presence·livekit
→ 권고: management-api.yaml v1.1로 구현에 맞춰 갱신.

---

## 5. 우선순위
- **즉시(문서, 코드 무관)**: §3 D26 문서 드리프트 갱신 + §4 계약 v1.1 정렬.
- **HIGH(외부 의존)**: STT(S2 PoC), AI 초안 생성, 실 ERP 전송/DB — Phase 5~6 외부 리소스 필요.
- **MED(현 범위 가능)**: 회의 예약 충돌 검증, PUT /seat-assignments, WAM areas 배선, D20 배치(30일 파기).

## 6. 종합
- 기획 대비 **핵심 기능·화면·계약은 대부분 구현**(스케줄러·KPI·좌석편집기·7화면 정합 포함). pytest 412 / build EXIT=0 / 라이브 QA 8/8.
- 실제 잔여는 **① 외부 의존(STT·AI·실ERP·WA 고급) ② 문서 드리프트(D26 3d-design/spec) ③ 계약 문서 정렬 ④ 소규모 MED(예약충돌·PUT배정·D20배치)**.
- 워커 자동 감사만으로는 false-negative 위험 → 본 리포트가 실측 정정본.

---

## 7. 조치 완료 (2026-07-07, 후속 실행)
audit 실행가능 항목 전량 처리:
- **§3 D26 문서 드리프트**: docs/3d-design/{scene-structure,asset-registry,optimization-criteria}.md + 02-trd·11-tech-stack·01-prd 상단에 D26 전환 배너 추가. (virtual-office-3d.yaml은 기존 배너 확인)
- **§4 계약 정렬**: docs/api/management-api.yaml description에 구현 정합 부록(경로/메서드 드리프트 + 미문서 엔드포인트 전량 목록) 추가.
- **§2 MED 코드**:
  - **PUT /api/seat-assignments/{seat_id}**(관리자 재배정, history 승계) 구현 + 테스트.
  - **presence 30일 좌표 파기**(D20-a): scheduler `_presence_purge_job` 매일 03:00 KST(x/y/z NULL) + 테스트.
  - **WAM areas 배선**(D24): map_generator.generate_wam(zones→focusable area, mapUrl 포함) + generate_and_upload_map 번들. **map-storage 업로드 200, org-map.wam 6 areas 실검증.**
  - **회의 예약 충돌 검증(D23)**: 실측 결과 **이미 구현됨**(meetings.py 409 room_time_conflict) — audit false-negative 정정.
- 검증: pytest **417 passed**(+5), 스케줄러 4잡(presence_purge 포함) lifespan 확인, WAM 업로드 실검증.
- **잔여(외부 의존, 미착수)**: STT(S2 PoC·엔진), AI 서술초안 생성(Claude), 실 ERP push 전송/DB, WA Room API 고급 scripting — 외부 리소스 필요.
