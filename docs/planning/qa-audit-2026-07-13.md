# QA 감사 리포트 — 전체 메뉴 기획 정합성 (2026-07-13)

## ✅ 처리 현황 (2026-07-13 당일 수리 완료분)

> 검증: 백엔드 pytest 327 passed(신규 테스트 15+ 포함), 프론트 tsc 0·build 23/23·전 라우트 200, 라이브 API 스모크(회의 예약→겹침 409→시작→종료, KPI ±10% 강제, EOD 타인 명의 403, 근태 본인 스코프) 통과.

**수리 완료**: §1 P0 전체 9건 (KPI 권한·±10%·30자·팀스코프·finalize admin 전용 / EOD 본인 강제 / 근태 admin 스코프 / aborted enum / 회의 예약 3중 결함: rooms 피커 API+UI·시간대 겹침 충돌검사·duration 도입 / 진행중 오버레이 status 쿼리 / Meeting 계약 통일 / presence working 7종+우패널 실배선). §2 중 10·11·12·16 (회의 상태전이 start/end·정원·leave / 동의 본인조회·거부 경로·STT 거부자 제외 게이트 / 이의 7일 창 정방향화+무이의 자동확정 배치+resolve 확정·재push / EOD row 자동생성·주말 스킵·재시도 상한). §3 정합 17~20·23~25·27 (역할 집합 정리, company_id INTEGER, id 타입, KST 날짜 유틸, 에러코드 한국어 매핑, attachments/issues, ActionItem 4상태, ai_draft 분기 한정). §4 문서 (04 v1.4 — 테이블 6종 등재·정정, 06 §3.15~3.17 신설). §5 다수 (공지 수정+감사기록+예약게시, 감사로그 날짜필터+old_value, CSV RFC4180, KPI 조정 모달·이의 카테고리 enum·D-n 표시, 오늘의 일정 KST 필터, 입장 실패 표시, 외근 필터 offline 분리).

**미수리 (후속 과제, 규모 순)**: ① 자율좌석 뷰포트 배선(§2-13: 서버·API 준비됨, 뷰포트 좌석 렌더/클릭/sit_request 발신 + 자동 반납 배치 + realtime claim 엔드포인트 안내 불일치) ② 좌석 편집 draft 격리 재설계(§2-14) ③ 주간/월간 자동 집계 리포트(§2-15, 06 §3.6) ④ 회의 참석자 초대/응답·캘린더 뷰·회의록 휴지통 ⑤ 셸 잔여(검색/알림, 내 상태변경 UI — realtime setStatus 노출 필요, 3카드 hidden 결정, WSS 단절 오버레이, 층 선택, 뷰포트 회의실↔회의 매핑) ⑥ KPI 관리자 보조요소(추이·팀 벤치마크·±30% 경고)·quarterly_total 산식 정본화 ⑦ notice↔announcement(04 §2.7) 스키마 개편 ⑧ presence NN 필드 복원 ⑨ /refresh 토큰 typ 구분 ⑩ 세션 만료 임시저장, 아바타 색상 외형(스프라이트 한계).

**신규 발견 (dev 환경)**: 매시간 ERP 동기화 배치가 mock 리더(샘플 5계정)로 전체 대사를 수행해 **시드 계정(alice/bob/charlie)을 soft-delete** — `scripts.seed_dev` 재실행으로 복구했으나, 근본 해결(mock 리더에 시드 포함 또는 dev에서 대사 비활성화) 필요. dev.db에 Room 미시드였던 것도 본사(HQ) 1F + 회의실 3종 시드로 해소.

## ✅✅ 후속 과제 처리 (2026-07-13 2차 — /goal 후속 라운드)

위 미수리 목록의 후속 처리 결과:
- **① 자율좌석**: 뷰포트 좌석 렌더+클릭 착석/반납(REST: `/api/seats` + `/api/seat-assignments[/{id}/release]`) 배선, 자율좌석 **자동 반납 배치**(scheduler 매시 :30 — offline/12h 미활동), realtime `sit_ok` confirmWith 문자열 정정(`/api/seat-assignments`). sit_request 게이트 경유는 서버 레이아웃 시드 의존으로 REST 경로 채택(주석 기록). dev.db에 1F 자율석 4종 시드. ✅
- **② 좌석 편집 draft 격리**: 즉시 API 호출 → 로컬 pending 버퍼+[모두 저장]/[변경 취소]로 전환, 버전 목록에 **롤백 버튼** 배선. 완전한 layout-JSON 기반 격리 재설계는 P7 하드닝 과제로 유지(06 §3.3 스코프 주석). ◐(부분)
- **③ 자동 집계 리포트**: summary API에 `total_meeting_minutes`(회의 참석 분)·`categories_minutes`(카테고리별 시간) 확장 + work-status에 회의시간 카드·시간 기준 분포·**CSV 내보내기(RFC4180)**. ✅
- **④ 회의**: 참석자 **초대 API+UI**(user_name·멱등)·**응답(수락/거절) API+UI**·상태 아이콘(✓/✕/?)·`invite_status` 컬럼, **캘린더 월 뷰** 토글, **회의록 휴지통**(DELETE, draft만·확정본 409). ✅
- **⑤ 셸**: 헤더 **검색**(우패널 직원 필터)·**알림 드롭다운**(최근 공지+미확인 뱃지)·메시지 버튼→/chat, 뷰포트 **회의실↔회의 매핑**(rooms API 이름 매칭), **내 상태변경 UI**(뷰포트 상태 칩 → 7종 수동 전환, realtime setStatus 노출), **WSS 재연결 백오프+단절 오버레이**, 세션 만료 **returnTo 복귀**. 층 선택은 단일 층 플레이트라 무기능 유지(다층은 P2 렌더 파이프라인 이후). ✅(층 제외)
- **⑥ KPI**: 조정 모달에 지난 분기 **추이·팀 평균 벤치마크·±30% 급변 경고**. quarterly_total 산식은 06 §3.4를 구현 정본(기간 집계×일수/91 스케일)으로 **문서 정정**. ✅
- **⑦ notice↔announcement**: 04 §2.7에 구현 정합 노트(테이블명 notice·단일조직·author/created_by 2필드) — 멀티테넌트 전환 시 마이그레이션 예정으로 기록. ✅(문서)
- **⑧ presence NN**: 04 §2.4를 정정 — x/y/z는 D20-a 30일 파기(NULL 처리)와 양립 불가하므로 NULL 허용이 정본. ✅(문서)
- **⑨ /refresh**: 엔드포인트 **제거**(D4 단일 세션: 만료→재로그인. 발급 경로 없는 죽은 코드 + access 무한 갱신 통로). ✅
- **⑩**: 세션 만료 시 returnTo 경로 복귀 구현(폼 내용 임시저장은 페이지별 후속). 아바타 색상 외형 한계는 06 §3.9에 기록(스프라이트 리컬러/3D 전환 시 재개). ◐(부분·문서)
- **dev 풋건 근본 해결**: ERP 전체 대사에서 `password_hash` 보유 로컬 dev 계정 보존(sync.py) — 라이브 sync 후 재로그인 200 확인. ✅
- **3카드 hidden**: D29 시안 B 결정을 06 §1.2에 기록(뷰포트 전면 + 카드 기능의 도메인 화면 흡수). ✅(문서)

---

> 대상: 좌내비 10개 메뉴 + 관리 메뉴 6종 + 셸/뷰포트/프레즌스.
> 기준: 06-screens.md(화면 정본), 04-data-model.md(데이터 정본), 08-kpi-logic.md, 14-virtual-office-spec.md, 00-decisions.md(D13/D17/D18/D19/D20/D23/D24).
> 방법: 영역별 6트랙 병렬 감사(스펙 §단위 대조 + 코드 증거 file:line). 로드맵(10 §D27) P5~P7 범위 미구현은 '후순위'로 분리.

## 0. 총평

| 영역 | 발견 | High | 상태 한 줄 |
|---|---|---|---|
| 셸·뷰포트·프레즌스 | 21 | 5 | 이동·회의입장 코어는 견고, 프레즌스 표시·좌석 상호작용이 미배선 |
| 업무기록·업무현황·보고서 | 12 | 3 | CRUD 견고, '중단' 상태 버그·EOD 자동화 공백 |
| KPI (평가·대시보드·이의) | 18 | 2 | 상태머신 견고, **권한·검증 규칙 다수 미강제** |
| 회의·회의록 | 21 | 5 | **예약 코어가 실사용 불가 수준** — 최우선 수리 영역 |
| 관리자 화면 6종+인증 | 24 | 4 | 화면 골격 완성, 세부 스펙 요소·감사 기록 공백 |
| 데이터 모델 정합(횡단) | 18 | 3 | UTC/D18 준수 양호, **권한 집합 3원화·문서 드리프트** |

## 1. P0 — 즉시 수정 필요 (실동작 버그 · 권한 결함)

### 1.1 권한 결함 (보안)
1. **KPI 권한 집합 오류** — `kpi.py:45` `_ADMIN_ROLES={"admin","leader"}`:
   - super_admin이 KPI 조정·확정·이의 재검토에서 403 (모든 타 라우터는 super_admin 포함).
   - leader 팀 스코프 검사 없음 → 타 팀 임의 user의 점수 조정 가능 (06 §3.10: leader=자기 팀 한정).
   - finalize(확정)는 스펙상 admin 전용인데 leader 가능 (06 §3.10 매트릭스).
2. **±10% 조정 한도 미강제** — `kpi.py:122-124,266`: AdjustRequest에 범위 검증 없음, 0→100 조정 가능 (08 §3.2). revised_score(455-457행)도 동일. 조정 사유 ≥30자도 min_length=1로 격하.
3. **EOD 타인 명의 큐잉** — `erp.py:295,326`: `user_id=body.user_id or user.user_id`에 role 검사 없음 → 일반 직원이 타인 명의 ERP 일일보고 생성·전송 가능.
4. **근태 API 미게이트** — `erp.py:211-221`: GET /api/attendances가 인증만 요구 → 일반 직원이 전 직원 출퇴근 시각 조회 가능 (06 §3.12: 관리자 전용).

### 1.2 실동작 버그
5. **업무 '중단' 저장 실패** — `tables.py:185-188` WorkLogStatus에 aborted 없음, UI(work-log/page.tsx:669)는 '중단' 옵션 제공 → 선택 시 422. (06 §3.6은 aborted 명시)
6. **회의 예약이 사실상 불가** — 3중 결함:
   - room_id가 자유 텍스트(기본 'room-1')인데 백엔드는 UUID 요구 → 기본값 예약 항상 실패 (meetings/page.tsx:451,493). 방 목록/피커 API·UI 없음.
   - 충돌 검사(D23) 오작동 — `meetings.py:197-203` or_() 3분기: 같은 방에 미래 회의가 있으면 그보다 이른 예약 전부 409.
   - 종료 시각/duration 개념 없음 → 시간대 겹침 검출 불가.
7. **'진행중 화상회의' 오버레이 오표시** — `OfficeShell.tsx:364-366`이 미지원 쿼리 `?status=in_progress` 호출 → 예정·취소 포함 모든 회의가 LIVE 표시.
8. **회의 데이터 계약 불일치** — OfficeShell.tsx:53-60 `start_time/end_time/participant_count` vs 백엔드 `scheduled_at/started_at/ended_at` → 오늘의 일정 "Invalid Date". meetings 페이지 STATUS_LABEL 키도 `started/ended` vs 백엔드 `in_progress/completed`.
9. **프레즌스 표시 결함 2종**:
   - StatusBadge 7종 중 `working` 누락(StatusBadge.tsx:6-12) → working 사용자가 offline 뱃지로 표시, 우패널 필터에서도 누락.
   - 우패널이 존재하지 않는 `GET /api/presence/employees` 호출(404) → `/api/employees` 폴백 후 전원 'online' 하드코딩(OfficeShell.tsx:380-391). erp.py가 반환하는 presence_status 무시.

## 2. P1 — 스펙 핵심 플로우 공백

10. **회의 상태 전이 부재** — 시작/종료 엔드포인트 없음 → in_progress/completed 도달 불가, started_at/ended_at 영구 NULL (`meetings.py:220,466`). join 시 상태·정원 검사 없음, left_at 미기록.
11. **녹음/STT 동의 게이트 부재 (D20-b 컴플라이언스)** — LiveKit 입장이 동의 확인 없이 연결(OfficeShell.tsx:287-297). '오디오 없이 참여' 거부 경로 없음. 참석자 본인 동의 상태 조회 API도 호스트 전용(consent.py:125-129) → 새로고침 시 동의 여부 유실. STT 게이트 의미도 역전: 스펙=거부자만 제외, 구현=1명 미동의 시 전체 403(meeting_minutes.py:400-420).
12. **이의신청 7일 창 타이밍 역전** — 스펙: 공개 후 7일 내 이의 → resolved 시 확정. 구현: 확정 후에만 이의 접수(`kpi.py:365-381`), resolve 시 finalized_at 미갱신. 7일 무이의 자동확정 배치도 없음(scheduler.py). 확정 즉시 ERP push 적재(이의 창 개시 전) + 재조정 시 재push 없음.
13. **자율좌석 상호작용 미배선 (06 §3.11)** — 뷰포트에 좌석 렌더/클릭 없음. realtime sit_request 핸들러(OfficeRoom.ts:309-340)·백엔드 API(seats.py)는 준비됨 — 프론트만 공백. 퇴근/장기 away 자동 반납 경로 없음. realtime이 안내하는 확정 엔드포인트 `POST /api/seats/claim`도 실제(`/api/seat-assignments`)와 불일치(OfficeRoom.ts:340).
14. **좌석 편집 draft 격리 붕괴** — 드래그/생성/삭제가 즉시 운영 seats 테이블에 반영(office-layout/page.tsx:154-183), 레이아웃 버전은 사후 스냅샷 → 검증·배포·롤백 상태머신(06 §3.3) 무력화. 롤백 백엔드는 있으나 UI 버튼 없음. draft 3D 프리뷰 없음. 팔레트 도구 4종만(Door/Spawn/Trigger 등 하드코딩 자동생성).
15. **주간/월간 자동 리포트 미구현** — 06 §3.6: work_log 자동 집계(회의시간 합산·시간 기반 카테고리 분포·상태 요약 편집·CSV). 현재 reports는 자유텍스트 수기 작성으로 대체 — 스펙과 다른 물건. 신규 report와 KPI/EOD 체계 간 관계도 미정의(일일보고 이중 체계 위험).
16. **EOD 자동화 공백 (D17)** — 배치가 push row 자동 생성 안 함(폼 미제출 날은 푸시 없음, eod_push.py:67-74), 주말·공휴일 스킵 없음, MAX_RETRY=3 선언만 되고 미사용. 직원 본인 전송상태 확인 UI 없음(GET이 admin 전용), 재제출마다 중복 pending row.

## 3. P2 — 데이터 정합성

17. **관리자 역할 집합 3원화 (횡단)** — `{"admin","leader"}`(kpi, meeting_minutes) / `{"admin","super_admin"}`(chat·consent·directory·office_layouts·erp·audit·notices) / `{"admin","super_admin","leader"}`(trips·reports·work_logs·meetings·seats). 모듈별 임의 → 단일 정의(core/deps.py 상수)로 통일 필요.
18. **company_id 타입 충돌** — erp_user.company_id=Integer vs org_group/office=UUID(tables.py:334,411) — 같은 테넌트 스코프인데 조인 불가 (04 §2.2: INTEGER 정본).
19. **id 타입 계약 위반 (런타임 무증상)** — Trip.id·Report.id 프론트 `number` vs 백엔드 UUID 문자열.
20. **날짜 경계 KST 미준수 (D19 표시 규칙)** — 프론트 `toISOString()` UTC 날짜 → KST 00~09시에 '오늘'=전일(work-log:64, work-status:54). 백엔드 `date.today()` 서버 로컬. D17 '18:00 이후 익일 귀속' 규칙 미구현.
21. **notice vs announcement(04 §2.7 정본) 괴리** — 테이블명·company_id 부재·body nullable·author 자유문자열(임의 작성자명 가능).
22. **presence 필수 필드 nullable화** — 04 §2.4 NN 정본이 전면 Optional로 완화(tables.py:754-768).
23. **에러코드 미매핑** — 백엔드 detail 코드(`trip_not_editable` 등)가 JSON 원문 그대로 사용자 노출(lib/api.ts:40-43).
24. **attachments·issues 전 구간 누락** — 테이블엔 있으나 API 스키마·폼에 없음 → 파일 첨부·이슈 기록 불가(work_logs.py:44-92).
25. **ActionItemStatus 이진 표시** — 백엔드 4상태 중 completed 외 전부 '진행'(cancelled 포함).
26. **quarterly_total 산식** — 스펙 '13주 합산' vs 구현 `score×(일수/91)` 스케일(kpi_engine.py:411-420).
27. **ai_draft 구조 불일치** — 08 §6.2.2 구조(strengths[]/overall/team_percentile) vs 평면 `{강점,개선,근거}`; 프론트는 JSON.stringify 원문 노출. daily에도 초안 생성(스펙: 분기만). D16 폐기된 weekly/monthly 키 잔존.

## 4. 문서 미갱신 (04-data-model.md 등)

- 신규 3테이블(business_trip/report/chat_message) + 기존 3테이블(recording_consent/erp_sync_log/user_avatar) 총 **6종이 04 정본에 미등재**.
- work_log.completed_at, erp_user.password_hash(D20-f 긴장) 미문서화.
- kpi_result.ai_model 기본값('claude-opus' vs 'mock'), asset gltf_path 전환 완료(문서 경고가 역으로 스테일).
- **출장관리·커뮤니케이션·보고서(수기)는 06에 상세 스펙 섹션 자체가 없음** — 좌내비 이름만 존재. 구현이 스펙 근거 없이 상태머신을 자체 확정(→ 06 §3.15~3.17 추가 필요).
- 커뮤니케이션 구현 스코프가 기획 원칙(§2: "회의 메모·근접 DM 수준, 본격 채팅은 Phase 7") 초과 — 채널 채팅 선구현. 기획 결정 재확인 또는 문서 개정 필요.

## 5. UI 스펙 미달 (Med/Low 발췌)

- 셸: 헤더 검색/알림 '준비중', 내 상태 하드코딩('online' 고정, 상태변경 UI 없음 — realtime setStatus 미노출), 3카드 행 hidden(시안 B 결정 vs 06 정본 드리프트), WSS 단절 오버레이 대신 칩+로컬 폴백, 층 선택 무기능, 아바타 HUD에 직급·상태점 없음, 회의 입장이 roomId 무시하고 meetings[0] 고정, 입장 실패 무음 처리.
- 회의: 참석자 초대/응답 없음(self-join만, role 항상 PARTICIPANT), 캘린더 뷰·검색 없음, 회의록 휴지통/재작성 없음, 녹화 기능 없음(P5).
- KPI: 관리자 검토 화면 보조 요소 일괄 부재(추이·팀 벤치마크·±30% 경고·prompt 2개로 조정), 직원 대시보드 추이 차트·비교·내보내기 없음, 이의 D-n 표시 없음, 이의 처리 감사 로그 없음, 감사 old_value 미기록.
- 관리자: 공지 수정 기능·감사 기록·예약 게시 노출 없음, 동기화 콘솔 배치 상태·EOD run 트리거 없음, 직원명부 상세 필드 미달(Slack/GitHub 컬럼 자체 부재), CSV RFC4180 미준수, 감사로그 날짜 필터 없음, 세션 만료 시 폼 유실, /refresh 죽은 코드(typ 미구분 — access로 무한 갱신 가능 스킴), 아바타 색상 외형 미적용(스프라이트 한계 주석 있음).
- 업무: 기간 탭 축소(지난주·연간 없음), '외근·출장' 필터에 offline 혼입(OfficeShell.tsx:433).

## 6. 후순위 (로드맵 정합 — 결함 아님)

- P5: 화상 참석자 타일(2×2)·녹화(Egress)·미디어바 확장.
- P6: STT 자동 초안(현재 명시적 501 — 정합), KPI 워크플로우 화면 고도화 잔여.
- P3/고도화: 조직도 편집기 잔여(team_zone 매핑·노드 편집·Undo).
- Phase 7: 본격 채팅(DM·스레드 등 — 현 구현은 이미 이 방향으로 반 발 나감, §4 결정 필요).

## 7. 권장 수리 순서

1. **권한 핫픽스** (반나절): §1.1 4건 — 역할 집합 단일화 + ±10%/팀 스코프 강제 + EOD 본인 강제 + attendances 게이트.
2. **동작 버그** (1일): §1.2 — aborted enum, 회의 예약(방 피커+충돌 검사 수정+상태 쿼리 지원), Meeting 계약 통일, presence working/우패널 배선.
3. **플로우 공백** (수일): §2 — 회의 상태 전이, 동의 게이트, 이의 7일 창, 좌석 draft 격리, EOD 자동화.
4. **문서 정합** (반나절): 04/06 갱신 + 커뮤니케이션 스코프 결정 기록.

---
*생성: 2026-07-13, 6트랙 병렬 감사 종합. 증거는 감사 시점 file:line.*
